{
  "name": "agenda-monorepo",
  "private": true,
  "scripts": {
    "backend": "mvn -f backend/pom.xml -pl agenda-service spring-boot:run",
    "frontend": "npm --prefix frontend run start",
    "build": "npm --prefix frontend run build && mvn -f backend/pom.xml -pl agenda-service package -DskipTests"
  }
}
